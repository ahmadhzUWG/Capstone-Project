@{
    ViewData["Title"] = "Home Page";
    Layout = null;
    string userRole = ViewBag.UserRole ?? "Guest";
}

<link rel="stylesheet" href="~/css/home.css" />

<main class="home-container">
    <div class="card">
        <h2>Welcome to the Home Page</h2>
        <p>Select an option below:</p>

        <div class="button-container">
            @if (userRole == "Admin")
            {
                @Html.ActionLink("Manage Employees", "Index", "Admin", null, new { @class = "btn btn-primary" })
                @Html.ActionLink("Manage Groups", "Index", "Group", null, new { @class = "btn btn-primary" })
                @Html.ActionLink("Manage Projects", "Index", "Admin", null, new { @class = "btn btn-primary" })
            }
            
            @Html.ActionLink("Logout", "Login", "Login", null, new { @class = "btn btn-danger" })
        </div>
    </div>
</main>